<Window x:Class="Presonus.UCNet.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presonus.UCNet.Wpf.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="1600" Width="900" Background="{StaticResource Background}">

    <Grid>
        <TabControl>
            <!-- MixerView tab -->
            <TabItem Header="MixerView">
                <local:MixerView DataContext="{Binding}" />
            </TabItem>

            <TabItem Header="MeterView">
                <local:MeterView DataContext="{Binding}" />
            </TabItem>

            <TabItem Header="ChannelDetailView">
                <local:ChannelDetailView DataContext="{Binding}" />
            </TabItem>
        </TabControl>
    </Grid>
</Window>